import React from 'react';

const SceneViewer = () => {
  return (
    <div style={{
      height: '100%',
      width: '100%',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      backgroundColor: '#f4f4f4',
      color: '#666',
      fontSize: '1.2rem'
    }}>
      Scene Viewer Placeholder (Coming Soon)
    </div>
  );
};

export default SceneViewer;
